import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  input: {
    marginTop: 50,
    marginHorizontal: 20,
    borderWidth: 1,
    paddingLeft: 20,
    width: '90%',
    fontSize: 20,
  },
});
